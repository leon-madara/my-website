# Cloudflare Pages redirects and rewrites
# Format: /source /destination status_code
# Status 200 = rewrite (keeps URL), 301/302 = redirect (changes URL)

# Clean URLs - rewrite to .html files (keeps URL in browser)
/about /about.html 200
/contact /contact.html 200
/portfolio /portfolio.html 200
/edumanage /edumanage.html 200
/profile-card /profile-card.html 200
