# .agent Notes

This folder is now a lightweight companion to the repo workflow.

- Keep durable project guidance in root `AGENTS.md`, `WORKFLOW/`, and `docs/exec-plans/`.
- Keep `.agent/` limited to small behavior or tool hints that help agent-capable editors.
- When `.agent` guidance affects a real implementation decision, record that decision in the relevant execution-plan folder instead of only here.

See `WORKFLOW/02_CODEX_INTEGRATION.md` for the full context precedence order.
