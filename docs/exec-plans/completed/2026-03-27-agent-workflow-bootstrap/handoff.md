# Handoff

## Next Recommended Step

For the next medium or complex build, create `docs/exec-plans/active/<feature-slug>/` from the template and link any relevant `.kiro/specs/<feature>/` documents in that folder's `README.md`.

## Notes

- The workspace baseline now lives at `C:\Users\Leon\DevMode\AGENTS.md`
- Start future sessions from root `AGENTS.md`
- Read `WORKFLOW/02_CODEX_INTEGRATION.md` before changing the context-layer strategy
- The local branch-persistence safety net lives under `.git/info/agent-workflow-bootstrap/`
- Legacy reference docs still exist and can be consulted when implementation details are missing from the newer workflow docs
