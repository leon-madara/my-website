# TODO

- Review screenshots for layout/motion polish (desktop/tablet/mobile + reduced motion).
- Add reveal transitions keyed off `data-visible` (if we want the scroll-reveal effect).
- Decide whether to keep “Schedule a Meeting” as placeholder toast or provide a Calendly URL.
- After sign-off: switch `/contact` to point at `ContactRebuildRoute` and delete/retire old `ContactRoute`.
