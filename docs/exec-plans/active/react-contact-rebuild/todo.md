# TODO

- Implement `ContactRebuildRoute` and styles.
- Add tokens (spacing/radius/motion) and container contract.
- Add scroll-reveal system (IntersectionObserver) and disable under reduced motion.
- Add RTL tests for validation + success flow.
- Add screenshot script + baseline captures (ignored outputs).
- Run targeted verification (`npm test`, `npm run react:test`) and record.

