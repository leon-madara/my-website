# TODO

- Optional: replace "Schedule a Meeting" placeholder with a real scheduling URL (or keep the toast).
- Optional: do one final live eyeball pass on hero spacing after merge if the fixed footer/nav chrome changes again.
