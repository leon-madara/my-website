# Todo

- Review and refine copy tone (more "Leon", less generic).
- Decide if we want a dark-theme tuned version of the page (v1 currently relies on global dark styles).
- Add real visuals:
  - screenshot frenzy gallery images
  - AI exploration concept variants
- Expand keyword artifact mapping (more triggers, better previews).
- Manual browser check on desktop + mobile.
- Fine-tune SVG hero:
  - decide contain vs cover for the mural on small screens
  - ensure mobile bottom nav doesn't hide any critical parts (or add safe padding)
  - optional: add a subtle "Scroll" hint / affordance
