# Plan

## v1 Implementation
1. Add new page `public/design-process.html` with the agreed narrative sections (Hero -> CTA).
2. Add new styles `public/css/design-process.css` for an epic but readable baseline.
3. Add `public/js/design-process.js` for:
   - keyword artifact popovers
   - soft scroll reveals
4. Update liquid nav on `index.html`, `about.html`, `portfolio.html`, `contact.html` to include `design-process.html` between `portfolio.html` and `contact.html`.
5. Update mobile bottom nav blocks (where present) to include `Process` between `Portfolio` and `Contact`.

## v2 Upgrades (Later)
- Replace placeholder frames with real project screenshots / generated concept images.
- Expand artifacts and make them feel more "alive" (micro-animations, richer previews).
- Add a lightweight section index/sticky progress indicator (if desired).
